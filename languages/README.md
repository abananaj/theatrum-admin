# Translations

Place `.po`/`.mo` files here, named `theatrum-admin-{locale}.mo` (e.g. `theatrum-admin-es_ES.mo`).

Loaded via `load_plugin_textdomain('theatrum-admin', ...)` in `theatrum-admin.php`.
