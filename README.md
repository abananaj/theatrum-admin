
# Functions to add

1. The ability to click a button in the block inspector to reset ALL custom styles applied to the block in the block editor. It should NOT delete custom additional classes added (unless they are classes automatically put there by gutenberg). It should delete any custom css the user entered in the block editor for the block instance. 

2. A function that creates a tooltip when hovering over a block in the list & outline view sidebar on the left. The tooltip will display the custom css classes added by the user in the block editor for that block instance. This will help users see at a glance which blocks have custom styles applied to them without having to click into each one.

3. A function that adds a small CSS indicator next to a block's name in the list & outline view that indicates the block instance has custom css.npm install
npm run build